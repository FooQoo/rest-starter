spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: r2dbc:h2:mem:testdb
    username: sa
    password:
  h2:
    console:
      enabled: true
  sql:
    init:
      mode: always

logging:
  level:
    org.springframework.r2dbc: DEBUG